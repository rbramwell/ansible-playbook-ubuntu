---

- hosts: php
  remote_user: root
  tasks:
    - name: stop all nodes
      service:
        name: "{{ item.name }}"
        state: "{{ item.state }}"
      with_items:
        - { name: "php5.6-fpm", state: "stopped" }
        - { name: "php7.0-fpm", state: "stopped" }
